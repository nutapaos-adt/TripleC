# v1-full-coc-flow

**Date:** 2026-08-21

**Driven by:** No standalone Requirement/Backlog/Feature List/User Journey doc existed for this yet. This
version was reverse-engineered from the current Triple C codebase — `SETUP.md` (informal feature/completion
log), `DESIGN.md` (design system + component patterns), and the actual data model/routes in
`app/`, `database/migrations/`, and `routes/web.php` — then confirmed with the user as the full 12-screen
scope covering the entire continuity-of-care loop plus admin.

**Screens included:**

| File | Screen | Flow |
|---|---|---|
| `dashboard.html` | Team dashboard (KPI tiles, today/overdue follow-ups, risk signals) | Dashboard |
| `referrals-list.html` | Referral/case list | Case intake |
| `referral-create.html` | New referral intake form | Case intake |
| `referral-detail.html` | Referral detail hub (patient info, timeline, attachments) | Case intake |
| `care-plan-confirm.html` | AI-drafted care plan → nurse confirmation | AI care-plan confirmation |
| `followup-guide.html` | AI-suggested pre-visit/pre-call guide | Home-visit / phone follow-up |
| `followup-record.html` | Record follow-up outcome | Home-visit / phone follow-up |
| `review-decide.html` | AI risk analysis + mandatory nurse decision (repeat/refer/close) | Risk analysis + nurse decision |
| `admin-case-types-list.html` | Case types list | Admin |
| `admin-case-type-form.html` | Case type create/edit (fixed-count vs score-based visit rules) | Admin |
| `admin-users-list.html` | Users list | Admin |
| `admin-user-edit.html` | User role/department edit | Admin |

**Design decisions confirmed with user:**
- Full 12-screen scope in one pass (not split into smaller batches).
- Prototype follows the DESIGN.md spec fully, including the sidebar navigation (dark olive gradient) and
  patient-history timeline component — both specified in DESIGN.md but not yet implemented in the current
  Blade views. This prototype is intentionally ahead of the current app's actual UI.

**Revisions after initial review:**
- 2026-09-07: `referral-create.html` — added "ผู้ดูแลหลัก" (primary caregiver name + phone) fields under
  ข้อมูลผู้ป่วย, and a "ประวัติการผ่าตัด (ถ้ามี)" field under ปัญหา/อาการปัจจุบัน, per user feedback that the
  original intake form was missing this data. Not yet reflected in the real `referrals/create.blade.php` or
  the `patients`/`referrals` schema — would need new columns + validation if/when built for real.
- 2026-09-08: `referral-create.html` — แหล่งที่มา/รายละเอียดแหล่งที่มา เปลี่ยนจากเลือกเองเป็นดึงอัตโนมัติจาก
  บัญชีผู้ใช้งานที่ล็อกอิน (read-only, เก็บค่าไว้ใน hidden input); ประเภทเคส (case_type) เปลี่ยนจาก
  ให้ AI ประเมิน+ตัวเลือกเดิม เป็นเลือกเองบังคับ ด้วยรายการแผนก (อายุรกรรม/ศัลยกรรม/กระดูกและข้อ/
  กุมารเวชกรรม/หลังคลอด/Palliative Care/อื่นๆ); เพิ่มช่อง "อุปกรณ์ของผู้ป่วย" แบบ checkbox เลือกได้หลายรายการ
  (NG-Tube, TT-Tube, Foley cath, Colostomy bag, Oxygen, ไม่มี) พร้อมช่องอื่นๆ ระบุเอง ใต้หัวข้อ
  ปัญหา/อาการปัจจุบัน. เช่นเดียวกับรายการก่อนหน้า — ยังไม่ได้ทำใน Blade/schema จริง.
- 2026-09-08 (รอบ 2): `referral-create.html` — ย้ายฟิลด์ "ผู้ดูแลหลัก" (ชื่อ+เบอร์โทร) จากก่อนที่อยู่ ไปไว้
  หลังกลุ่มที่อยู่ทั้งหมด (ที่อยู่/ตำบล/อำเภอ/จังหวัด) ในหัวข้อข้อมูลผู้ป่วย; เพิ่มฟิลด์ "สิทธิการรักษา"
  (dropdown: บัตรทอง/ประกันสังคม/ข้าราชการ-รัฐวิสาหกิจ/ชำระเงินเอง/ประกันเอกชน/อื่นๆ) ต่อจากเบอร์โทรผู้ป่วย.
- 2026-09-08 (รอบ 3): `referral-create.html` — เพิ่ม "การวินิจฉัยโรค" (บังคับกรอก) และ "โรคประจำตัว (ถ้ามี)"
  เป็น 2 คอลัมน์แรกในหัวข้อปัญหา/อาการปัจจุบัน ก่อนประวัติการผ่าตัด/อุปกรณ์ของผู้ป่วย.
- 2026-09-08 (รอบ 4): `referral-create.html` — เพิ่ม 4 ฟิลด์ท้ายหัวข้อแหล่งที่มาของเคส: วันที่ Admit,
  วันที่จำหน่าย, วันที่นัดติดตามอาการ (OPD/แพทย์เจ้าของไข้ครั้งถัดไป), ชื่อแพทย์เจ้าของไข้.
- 2026-09-08 (รอบ 5): `referral-create.html` — ลดความยาวหน้าจอด้วยการเพิ่ม `.grid-3` (3 คอลัมน์ต่อแถว)
  แล้วจัดกลุ่มฟิลด์สั้นๆ ที่เกี่ยวข้องกันให้อยู่บรรทัดเดียวกัน: วันที่ Admit/จำหน่าย/นัดติดตาม เป็น 1 แถว,
  และ ตำบล/อำเภอ/จังหวัด เป็น 1 แถว (จากเดิมกินพื้นที่ 2 แถวครึ่งในโครง grid-2 เดิม). ปรับ media query
  ให้ grid-3 ยุบเหลือ 2 คอลัมน์ที่หน้าจอ 861–1100px และ 1 คอลัมน์ที่ ≤860px เหมือน grid-2.
- 2026-09-08 (รอบ 6): `referral-create.html` — เพิ่มฟิลด์ "อายุ" ที่ลืมไป โดยคำนวณอัตโนมัติจากวันเดือนปีเกิด
  (แสดงเป็น hint ใต้ช่อง ไม่ใช่ช่องกรอกแยก); จัดเรียงหัวข้อข้อมูลผู้ป่วยใหม่เป็น HN/เลขบัตร → ชื่อ-นามสกุล/
  สิทธิการรักษา → วันเดือนปีเกิด(+อายุ)/สถานะผู้ป่วย → เบอร์โทร → ที่อยู่ ฯลฯ; เพิ่มฟิลด์ "สถานะผู้ป่วย"
  (กำลังพล/ครอบครัวกำลังพล/ประชาชน) — เมื่อเลือก "กำลังพล" จะแสดงช่อง "หน่วยต้นสังกัด" เพิ่มขึ้นมาอัตโนมัติ
  (ครอบครัวกำลังพลยังไม่ขอช่องนี้ตามที่ผู้ใช้ระบุ — ถามผู้ใช้แล้วว่าต้องการเพิ่มด้วยหรือไม่).
- 2026-09-08 (รอบ 7): `referral-create.html` — เปลี่ยน "อายุ" จาก hint text ใต้ช่องวันเกิด เป็นช่องกรอก
  (input) แยกต่างหาก แบบ readonly สีเทาอ่อน คำนวณอัตโนมัติเมื่อเลือกวันเกิด; ขยายเงื่อนไขการแสดงช่อง
  "หน่วยต้นสังกัด" ให้ครอบคลุมทั้ง "กำลังพล" และ "ครอบครัวกำลังพล" (เดิมมีแค่กำลังพล) พร้อม hint อธิบายว่า
  กรณีครอบครัวกำลังพลให้กรอกหน่วยของผู้มีสิทธิ; แก้ไข phone field ที่ซ้ำกันโดยไม่ตั้งใจจากรอบก่อนหน้าด้วย.
- 2026-09-08 (รอบ 8): `referral-create.html` — ย้าย "สถานะผู้ป่วย" (กำลังพล/ครอบครัวกำลังพล/ประชาชน)
  พร้อมช่อง "หน่วยต้นสังกัด" (conditional) จากหัวข้อข้อมูลผู้ป่วย มาไว้ในหัวข้อแหล่งที่มาของเคส
  ในกริดเดียวกับ ประเภทผู้ป่วย/เขตพื้นที่ — อยู่ใต้ "ประเภทผู้ป่วย (case_type)" พอดี.
- 2026-09-08 (รอบ 9): `referral-create.html` — เพิ่ม "ความสัมพันธ์กับผู้ป่วย" (dropdown: บุตร/ธิดา, คู่สมรส,
  บิดา/มารดา, พี่/น้อง, ญาติ, ผู้ดูแลจ้าง/ไม่ใช่ญาติ, อื่นๆ) คั่นกลางระหว่างชื่อผู้ดูแลหลักกับเบอร์โทร
  ผู้ดูแลหลัก โดยเปลี่ยนแถวนั้นจาก grid-2 เป็น grid-3; เอาตัวอย่าง "(บุตรชาย)" ออกจาก placeholder ชื่อ
  ผู้ดูแลหลัก เพราะย้ายไปเป็นฟิลด์แยกแล้ว.
- 2026-09-08 (รอบ 10): `referral-create.html` — แก้ปัญหาพื้นที่ว่างใต้ "ประเภทผู้ป่วย" ที่เกิดจาก CSS Grid
  บังคับให้ทุกคอลัมน์ในแถวเดียวกันสูงเท่ากล่อง เขตพื้นที่ (zone) ซึ่งเนื้อหายาวกว่า — แก้โดยจับกลุ่ม
  ประเภทผู้ป่วย/สถานะผู้ป่วย/หน่วยต้นสังกัด ไว้ใน column ซ้ายเดียวกัน (flex column อิสระ แยกจาก grid
  row-height ของ zone ทางขวา) ทำให้สถานะผู้ป่วยเลื่อนขึ้นมาชิดประเภทผู้ป่วยได้จริง ไม่ต้องรอความสูงของ
  zone box; หน่วยต้นสังกัดก็ย้ายมาอยู่ใน column นี้ด้วย (จาก span-2 เดิม เปลี่ยนเป็นความกว้างคอลัมน์เดียว).
- 2026-09-08 (รอบ 11): `referral-create.html` — เพิ่ม "การจำแนกผู้ป่วยตามระดับความรุนแรง" ใต้สถานะผู้ป่วย
  ในคอลัมน์ซ้ายเดียวกัน: กลุ่ม 1 บ้านสีเขียว (ช่วยเหลือตนเองได้ทั้งหมด), กลุ่ม 2 บ้านสีเหลือง (ช่วยเหลือ
  ตนเองได้บางส่วน), กลุ่ม 3 บ้านสีแดง (ช่วยเหลือตนเองไม่ได้เลย), กลุ่ม 4 Palliative Care — เพิ่ม chip
  แสดงสีตามกลุ่มที่เลือก (เขียว=success, เหลือง=warning, แดง=risk, กลุ่ม 4=primary tint) โดยใช้โทนสี
  semantic เดียวกับ badge อื่นๆ ในระบบ (never color alone — มีทั้งสีและข้อความกำกับเสมอ).
- 2026-09-08 (รอบ 12, ทั้งชุด 12 หน้า): เปลี่ยนสีหลักของทั้งระบบจากโทนเขียวโอลีฟ/ทหารเป็นโทนน้ำเงินอมเทา
  (Slate/Steel Blue) ตามฟีดแบ็กผู้ใช้ว่าโทนเขียวจ้องนานแล้วปวดตา — แก้ที่ `DESIGN.md` §1.3/§2.1 เป็นต้นทาง
  (hex ใหม่: primary-900 `#16232C`, 800 `#1E3644`, 700 `#2C5166`, 600 `#3D6B84`, 500 `#5586A0`,
  300 `#A8C4D2`, 100 `#E1EBEF`, 50 `#F4F8FA`, primary-dark `#8FC1D9`) แล้ว bulk-replace hex เดิมเป็นค่าใหม่
  ทั้ง 12 ไฟล์ในชุดนี้ และทั้ง 14 ไฟล์ Blade จริงที่แก้สีตาม DESIGN.md ไปก่อนหน้านี้ด้วย — เพื่อให้ prototype,
  DESIGN.md, และ Blade ยังคงสอดคล้องกันเป็นค่าเดียวกันทั้งระบบ. โทนเขียว (success), เหลือง (warning),
  แดง (risk) และสีรองจากโลโก้ (accent-blue/accent-gold) ไม่เปลี่ยน.
- 2026-09-08 (รอบ 13): เปลี่ยนข้อความปุ่ม/หัวข้อ "รับเคสใหม่" เป็น "ส่งข้อมูลเยี่ยมบ้าน" ทั้งใน
  `referrals-list.html` (ปุ่ม + รับเคสใหม่) และ `referral-create.html` (title + h1) — พร้อมแก้ไฟล์ Blade
  จริงคู่กัน (`referrals/index.blade.php`, `referrals/create.blade.php`) ให้ข้อความตรงกัน.
- 2026-09-08 (รอบ 14): ปุ่มบันทึกบนฟอร์ม `referral-create.html`/`referrals/create.blade.php` เปลี่ยนข้อความ
  เป็น "บันทึกและส่งข้อมูล" (เดิม "บันทึกและรับเคส" ในต้นแบบ, "บันทึกใบส่งต่อ" ใน Blade — รวมให้ตรงกัน);
  เพิ่มปุ่ม "แก้ไขข้อมูล" ที่มุมขวาบนของ `referral-detail.html` (แสดงเฉพาะกรณีสถานะยังเป็น "รอตรวจสอบ" —
  คือทีมเยี่ยมบ้าน/พยาบาลยังไม่ยืนยันแผนดูแล จึงยังแก้ไขข้อมูลต้นฉบับได้ — ระบุเงื่อนไขนี้ไว้เป็น comment
  ในไฟล์เพราะ prototype แบบ static ไม่มี state จริงให้ toggle) ปุ่มลิงก์ไปหน้าฟอร์มเดิม (`referral-create.html`)
  ชั่วคราวแทนที่จะมีหน้าแก้ไขแยก — ยังไม่ได้ทำในฝั่ง Blade จริงเพราะต้องสร้าง route/controller/edit-view ใหม่
  ทั้งหมด (ตอนนี้มีแค่ index/create/store/show) ถามผู้ใช้แล้วว่าต้องการให้สร้างจริงตอนนี้เลยหรือไม่ —
  ผู้ใช้ตอบว่ารอดูภาพรวม prototype ให้ครบก่อน.
- 2026-09-08 (รอบ 15): ปรับหน้า `referral-detail.html` ใหม่ทั้งหมด:
  1. เปลี่ยนเลย์เอาต์จาก `dl-grid` 2 คอลัมน์ เป็น `.info-list`/`.info-row` แถวเดียวเต็มความกว้างการ์ด
     (label คงที่ 220px ทางซ้าย, ค่าทางขวา) อ่านง่ายขึ้นโดยเฉพาะค่าที่ยาว เช่น ที่อยู่/ชื่อหน่วยงาน
  2. เพิ่ม "วันที่ส่งต่อข้อมูล" และ "ส่งข้อมูลโดย" (เดิม "วันที่รับเคส"/"รับเคสโดย" — ปรับคำให้ตรงกับ
     ปุ่ม/หัวข้อที่เปลี่ยนไปแล้วในรอบ 13) พร้อมแยกเป็นการ์ดใหม่ "แหล่งที่มาและการส่งต่อ" รวม ประเภทผู้ป่วย,
     วันที่ Admit/จำหน่าย/นัดติดตาม, ชื่อแพทย์เจ้าของไข้ ไว้ด้วยกัน
  3. เพิ่มข้อมูลที่ขาดไปในการ์ด "ข้อมูลผู้ป่วย": HN, สิทธิการรักษา, สถานะผู้ป่วย (+หน่วยต้นสังกัด),
     การจำแนกตามระดับความรุนแรง (พร้อม chip สี), และย้ายเบอร์โทร/ชื่อ/ความสัมพันธ์ผู้ดูแลหลักมาเป็น
     กลุ่มย่อยแยกจากข้อมูลผู้ป่วยเอง (เดิมเบอร์โทรผู้ดูแลถูกใส่ปนกับ "เบอร์โทร" ผู้ป่วยแบบไม่ชัดเจน)
  4. เพิ่มข้อมูลที่ขาดไปในการ์ด "ปัญหา/อาการปัจจุบัน": การวินิจฉัยโรค, โรคประจำตัว, ประวัติการผ่าตัด,
     อุปกรณ์ของผู้ป่วย (แสดงเป็น tag) — ใส่ไว้ก่อนบันทึกดิบเดิม (แยกหัวข้อย่อยชัดเจน)
  5. เปลี่ยนคำใน Timeline "รับเคสจาก..." → "ส่งข้อมูลเยี่ยมบ้านจาก..." ให้สอดคล้องกับคำใหม่
  ข้อมูลตัวอย่าง (mock) จงใจตั้งเป็นเคส "ครอบครัวกำลังพล" เพื่อให้เห็นว่าฟิลด์ "หน่วยต้นสังกัด" (conditional)
  แสดงผลอย่างไรในหน้ารายละเอียดจริง.
- 2026-09-08 (รอบ 16): เปลี่ยนข้อความปุ่ม "ตรวจสอบและยืนยันแผน(ติดตาม)" เป็น "วิเคราะห์แผนการพยาบาล"
  ทั้งใน `referral-detail.html` (ปุ่มในกล่อง AI-draft + ประโยคใน timeline ที่อ้างถึงการกระทำเดียวกัน)
  และหน้า Blade จริง `referrals/show.blade.php` ให้ตรงกัน.
- 2026-09-08 (รอบ 17): แก้ dropdown "ประเภทเคส" ใน `care-plan-confirm.html` ที่ยังใช้รายการเก่า
  (Palliative Care/หลังคลอด/หลังผ่าตัด/อื่นๆ) ไม่ตรงกับรายการใหม่ที่แก้ไปแล้วใน `referral-create.html`
  (รอบ 8) — อัปเดตให้ตรงกัน: อายุรกรรม/ศัลยกรรม/กระดูกและข้อ/กุมารเวชกรรม/หลังคลอด/Palliative Care/อื่นๆ
  (ยังเลือก Palliative Care ไว้เหมือนเดิมให้ตรงกับ mock data ของเคสนี้).
  หมายเหตุ: พบว่า `care-plan-confirm.html` ใช้ mock patient คนละชุดกับ `referral-detail.html`
  (HN 6801234/68 ปี เทียบกับ HN 660231/79 ปี — ชื่อเดียวกันโดยบังเอิญ) เพราะสร้างจากคนละ agent batch
  ตามที่บันทึกไว้ใน "Known prototype simplifications" — ยังไม่ได้แก้ในรอบนี้เพราะผู้ใช้ระบุแค่เรื่อง
  ประเภทเคส ถ้าต้องการให้ข้อมูลผู้ป่วยตรงกันทั้งชุดด้วย แจ้งได้.
- 2026-09-08 (รอบ 18): ทำให้ข้อมูลผู้ป่วย "นางสมหญิง ใจดี" ตรงกันระหว่าง `referral-detail.html` (ต้นทาง)
  กับ `care-plan-confirm.html` ตามที่ผู้ใช้ยืนยัน: HN (6801234→660231), อายุ (68→79 ปี), ที่อยู่/เขต
  (ต.ท่าสาย อ.เมือง → ต.บ้านดู่ อ.เมืองเชียงราย ตรงกับที่อยู่เต็มในหน้าใบส่งต่อ), แหล่งที่มา (ตัด "(ตึก 3)"
  ที่ referral-detail ไม่มีออก), ข้อความวินิจฉัย/ปัญหาหลักในกล่องร่าง AI (ระบุ Cholangiocarcinoma แทน
  "มะเร็งตับระยะที่ 4" ทั่วไป ให้ตรงกับการวินิจฉัยที่ระบุในหน้าใบส่งต่อ), และ "ข้อมูลต้นทาง (บันทึกดิบ)"
  เปลี่ยนจากข้อความสั้นคนละเวอร์ชัน เป็นข้อความเดียวกันกับ raw_notes ในหน้าใบส่งต่อเป๊ะๆ.
  ขอบเขต: แก้เฉพาะคู่หน้าที่อยู่ใน flow เดียวกันของเคสนี้ (ใบส่งต่อ ↔ ยืนยันแผน AI) — ไม่ได้แตะ
  `review-decide.html`/`followup-*.html` เพราะหน้าเหล่านั้นตั้งใจใช้ผู้ป่วยตัวอย่างคนละคน
  ("คุณสมชาย ใจบุญ") เพื่อโชว์สถานการณ์คนละแบบ (เคสที่เยี่ยมไปแล้วรอบหนึ่ง ไม่ใช่เคสใหม่).
- 2026-09-08 (รอบ 19): ย้ายจุดที่ถามหา PPS Score ครั้งแรก จาก "เฉพาะตอนยืนยันแผน" (care-plan-confirm.html)
  มาเป็น "ถามตั้งแต่ตอนส่งข้อมูลเยี่ยมบ้าน" (referral-create.html) ด้วย — ตามเหตุผลที่ผู้ใช้ชี้แจงว่า
  พยาบาลหอผู้ป่วยต้นทางเป็นคนเห็นสภาพผู้ป่วยจริง ในขณะที่ทีมเยี่ยมบ้านปลายทางไม่เห็นผู้ป่วยเลยตอนยืนยันแผน
  จึงควรให้ฝั่งที่เห็นผู้ป่วยจริงเป็นคนประเมินก่อน แล้วให้ทีมเยี่ยมบ้านตรวจสอบ/แก้ไขได้อีกครั้ง (ไม่ขัดกับ
  human-in-the-loop เพราะยังแก้ไขได้เสมอก่อนยืนยัน):
  - `referral-create.html`: เพิ่มช่อง "PPS Score (ประเมินโดยพยาบาลหอผู้ป่วย)" ในหัวข้อปัญหา/อาการปัจจุบัน
    แสดงเฉพาะเมื่อเลือกประเภทผู้ป่วยเป็น Palliative Care (toggle จาก case_type onchange เดียวกับที่มีอยู่แล้ว)
  - `care-plan-confirm.html`: ปรับ hint ของ PPS Score เดิมให้ระบุว่าเป็นค่าที่พยาบาลหอผู้ป่วยประเมินไว้แล้ว
    ตอนส่งต่อ (pre-filled) และทีมเยี่ยมบ้านตรวจสอบ/แก้ไขได้ก่อนยืนยัน — ไม่ใช่กรอกจากศูนย์เหมือนเดิม
  ยังไม่ได้แก้ฝั่ง Blade จริง (`referrals/create.blade.php`, `ReferralController::store()`,
  `referrals/care-plan.blade.php`) เพราะต้องเพิ่ม column/logic ใหม่จริงจัง — รอผู้ใช้ดูภาพรวมก่อนตามที่
  เคยตกลงกันไว้ในรอบ 14.
- 2026-09-08 (รอบ 20, consistency sweep): ผู้ใช้ขอให้ตรวจทั้งชุดว่ามีจุดไหนยังไม่ update ตามคำที่เปลี่ยนไป
  หลายรอบก่อนหน้า — grep หาคำเก่าทั้งโปรเจกต์แล้วพบและแก้:
  1. `referrals/show.blade.php` (Blade จริง): "รับเคสโดย" → "ส่งข้อมูลโดย" (ยังไม่เคยแก้ตอนรอบ 13/15
     เพราะตอนนั้นแก้เฉพาะปุ่ม ไม่ได้แก้ label นี้)
  2. `referrals/index.blade.php` (Blade จริง) และ `referrals-list.html` (prototype): หัวคอลัมน์ตาราง
     "วันที่รับเคส" → "วันที่ส่งต่อข้อมูล" ให้ตรงกับคำที่เปลี่ยนไปแล้วในหน้ารายละเอียด
  3. `admin-case-types-list.html`: เพิ่ม 4 แถวที่ขาดไป (อายุรกรรม/med, ศัลยกรรม/surg, กระดูกและข้อ/ortho,
     กุมารเวชกรรม/peds — slug ใช้ค่าเดียวกับ `<option value>` ใน `referral-create.html`) เพื่อให้ตัวเลือก
     ในหน้าส่งข้อมูลเยี่ยมบ้านมีอยู่จริงในหน้าจัดการประเภทเคสของแอดมินครบทุกตัว (เดิมมีแค่ Palliative Care/
     หลังคลอด/หลังผ่าตัด/อื่นๆ — ไม่ครบ) ยังคงแถว "หลังผ่าตัด" เดิมไว้ไม่ได้ลบ
  ยืนยันด้วย grep ทั้งโปรเจกต์แล้วว่าไม่มีคำเก่า ("รับเคส", "ตรวจสอบและยืนยันแผน", hex สีเขียวเดิม)
  หลงเหลืออยู่นอกเหนือจาก MANIFEST.md เอง (log ประวัติ) และ SETUP.md (เอกสาร build log แยกต่างหาก
  ไม่ใช่ UI จริง จึงไม่แตะ).
  หมายเหตุที่สังเกตแต่ยังไม่แก้ (นอกขอบเขตที่ขอ): ตัวเลือก "ประเภทผู้ป่วย" ตอนนี้ปนกันระหว่างการแบ่งตาม
  "แผนกต้นทาง" (อายุรกรรม/ศัลยกรรม/กระดูกและข้อ/กุมารเวชกรรม) กับ "สถานการณ์ทางคลินิก" (Palliative Care/
  หลังคลอด/หลังผ่าตัด) ในฟิลด์เดียวกัน ซึ่งเป็นคนละมิติของข้อมูลในทางออกแบบจริง — ถ้าจะแก้ต้องคิดเรื่อง
  data model ใหม่ ไม่ใช่แค่ sync ข้อความ จึงไม่ได้แตะในรอบนี้.
- 2026-09-08 (รอบ 21): ผู้ใช้ชี้แจงตรรกะจริงของจำนวนครั้งเยี่ยม — "ประเภทเคส" ที่มีอยู่ (อายุรกรรม/
  ศัลยกรรม/กระดูกและข้อ/กุมารเวชกรรม/หลังคลอด/Palliative Care) เป็นไปตามที่ตั้งใจแล้ว ไม่ต้องแยก field
  แต่จำนวนครั้งเยี่ยมจริงต้องพิจารณา "ประเภทเคส" ร่วมกับ "การจำแนกกลุ่มความรุนแรง" ตามลำดับความสำคัญ:
  1) Palliative Care → ตาม PPS Score, 2) หลังคลอด → 3 ครั้ง, 3) นอกเหนือจาก 2 ข้อบน + กลุ่ม 3 (บ้านสีแดง)
  → เยี่ยมเดือนละ 1 ครั้งต่อเนื่อง, 4) อื่นๆ ทั้งหมด → เยี่ยม 1 ครั้ง — ปรับ:
  - `admin-case-types-list.html`: แก้ "หลังคลอด" จาก 4 ครั้ง เป็น 3 ครั้ง (ห่างกัน 7 วันเท่าเดิม เพราะ
    ผู้ใช้ระบุแค่จำนวนครั้ง ไม่ได้ระบุระยะห่างใหม่); เพิ่มกล่องคำอธิบายลำดับความสำคัญทั้ง 4 ข้อไว้เหนือตาราง
    เพราะตารางเดิมโชว์กฎแยกตามประเภทเคสอย่างเดียว ไม่ครอบคลุมมิติกลุ่มความรุนแรงที่ตัดข้ามทุกประเภท
  - `care-plan-confirm.html`: ปรับข้อความ after-confirm-note ให้พูดถึง "ประเภทเคสร่วมกับกลุ่มความรุนแรง"
    แทนที่จะพูดถึงแค่ประเภทเคสอย่างเดียว
  ยังไม่ได้แก้ในเอกสาร/โค้ดจริง — `VisitRule`/`VisitPlanService` ตาม CLAUDE.md ผูกกฎไว้ที่ CaseType เท่านั้น
  ยังไม่มี dimension ของกลุ่มความรุนแรงในโมเดลข้อมูลจริง ถ้าจะทำจริงต้องออกแบบ schema/logic เพิ่ม (เช่น
  เงื่อนไข "กลุ่ม 3 ตัดข้ามทุกประเภท ยกเว้น Palliative/หลังคลอด" ในตาราง VisitRule หรือ service แยก) —
  รอผู้ใช้ดูภาพรวม prototype ให้ครบก่อนตามที่เคยตกลงกันไว้.
- 2026-09-08 (รอบ 22): ผู้ใช้ทักว่าคอลัมน์ "สรุปกฎการติดตาม" ในตารางยังไม่เปลี่ยนตามลำดับความสำคัญที่
  เพิ่งชี้แจง — พบว่ารอบ 20/21 เพิ่มกล่องอธิบายไว้เหนือตารางแล้ว แต่ลืมแก้ค่าในคอลัมน์ของแต่ละแถวเอง
  ซึ่งยังโชว์ตัวเลขที่ผมเดาไว้เองตอนรอบ 20 (เช่น "2 ครั้ง ห่างกัน 14 วัน") ที่ขัดกับกฎจริงที่เพิ่งชี้แจง
  แก้ให้ตรงแล้ว: อายุรกรรม, ศัลยกรรม, กระดูกและข้อ, กุมารเวชกรรม, หลังผ่าตัด, อื่นๆ (6 ประเภทที่ไม่ใช่
  Palliative Care/หลังคลอด) เปลี่ยนคอลัมน์นี้เป็น "1 ครั้ง (กลุ่ม 3 บ้านสีแดง: เดือนละ 1 ครั้งต่อเนื่อง)"
  ทั้งหมด ให้ตรงกับกฎข้อ 3-4 — เหลือแค่ Palliative Care (ตาม PPS) และหลังคลอด (3 ครั้ง) ที่มีค่าเฉพาะตัว.
- 2026-09-08 (รอบ 23): กลับไปตรวจ `care-plan-confirm.html` ตามที่ผู้ใช้ขอ พบและแก้ 2 จุดเพิ่มเติมที่
  รอบก่อนๆ พลาดไป:
  1. คอมเมนต์ CSS "/* Primary - Olive/Military Green */" ยังหลงเหลือจากก่อนเปลี่ยนสี (bulk replace
     ตอนนั้นแก้แค่ค่า hex ไม่ได้แก้คอมเมนต์) — แก้เป็น "Slate/Steel Blue" ทั้งในไฟล์นี้และ `dashboard.html`
     ที่เป็นไฟล์เดียวกันที่ยังมีคอมเมนต์นี้หลงเหลือ (อีก 10 ไฟล์ไม่มีปัญหานี้)
  2. Sidebar footer ระบุผู้ใช้ที่ล็อกอิน "พว. สุนีย์ วงศ์ประเสริฐ" (พยาบาลที่เป็นผู้ยืนยันแผนในหน้านี้)
     ผิดว่าเป็น "เจ้าหน้าที่หอผู้ป่วย (ward staff) — หอผู้ป่วยอายุรกรรมหญิง" ทั้งที่หน้านี้คือมุมมองของ
     ทีมเยี่ยมบ้านที่ยืนยันแผน (คนละบทบาทกับกัลยาที่ส่งข้อมูลมา) — แก้เป็น "ทีมเยี่ยมบ้าน (home visit
     team) — ทีมเยี่ยมบ้านชุมชน" ให้ตรงกับบทบาทจริงตามที่คุยกันไว้เรื่อง PPS Score (ward นางพยาบาลเห็น
     ผู้ป่วยจริง ส่วนทีมเยี่ยมบ้านยืนยันจากระยะไกล).
- 2026-09-08 (รอบ 24): เพิ่ม 2 แถวข้อมูลใต้ชื่อผู้ป่วยใน patient summary strip ของ `care-plan-confirm.html`
  ตามที่ผู้ใช้ขอ: แถว "โรคประจำตัว / การวินิจฉัย / การผ่าตัด" และแถว "วันที่ Admit / วันที่จำหน่าย /
  วันนัดติดตาม" (ค่าทั้งหมดตรงกับที่ตั้งไว้แล้วใน `referral-detail.html`) — โครงสร้างเดิมของ patient-strip
  เป็นแถวเดียว (name-block + meta-list + chip-row เรียงข้างกันหมด) จึงต้องปรับ `.patient-strip` จาก
  flex-row เป็น flex-column และแยก name+chip-row ออกมาเป็น `.patient-strip-top` ของตัวเอง แล้ววาง
  meta-list เดิม + 2 แถวใหม่ เรียงต่อกันเป็นบรรทัดถัดไป คั่นด้วยเส้นบางๆ ระหว่างแถว.
- 2026-09-08 (รอบ 25): ผู้ใช้ไม่ชอบเส้นคั่นระหว่างแถวที่เพิ่มไปตอนรอบ 24 อยากให้เป็นบรรทัดเล็กๆ ต่อเนื่อง
  เหมือนแถวแรก — เอา border-top ระหว่างแถวออก และลด gap ของ `.patient-strip` จาก space-4 เป็น space-2
  (เว้นระยะไว้เฉพาะใต้แถวชื่อ+chip ด้วย margin-bottom บน `.patient-strip-top`) ให้ทั้ง 3 แถว meta-list
  ดูเป็นชุดข้อความต่อเนื่องขนาดเดียวกัน ไม่มีเส้นแบ่งเป็นบล็อกแยกอีกต่อไป.
- 2026-09-08 (รอบ 26): เปลี่ยนฟิลด์ "กลุ่มผู้ป่วย / ประเภทผู้ป่วย" ในกล่องร่าง AI ของ `care-plan-confirm.html`
  (เดิมเป็นช่องข้อความอิสระ "ผู้ป่วยมะเร็งตับระยะสุดท้าย (End-stage...)" ที่ซ้ำซ้อนกับฟิลด์ "ปัญหาหลัก"
  อยู่แล้ว และไม่ตรงกับการจำแนกกลุ่มที่มีอยู่จริง) เป็น dropdown "การจำแนกกลุ่มความรุนแรง" ตัวเลือกเดียวกับ
  ที่ตั้งไว้ใน `referral-create.html`/`referral-detail.html` เป๊ะๆ: กลุ่ม 1 บ้านสีเขียว, กลุ่ม 2 บ้านสีเหลือง,
  กลุ่ม 3 บ้านสีแดง, กลุ่ม 4 Palliative Care — ตั้งค่าเริ่มต้นเป็นกลุ่ม 4 ให้ตรงกับที่ระบุไว้ในหน้าใบส่งต่อ
  ของเคสนี้ แก้ทั้งฟิลด์แก้ไขได้ในกล่องร่าง AI และค่าที่โชว์ในส่วนตัวอย่างสถานะหลังยืนยัน (confirmed preview).
- 2026-09-08 (รอบ 27): เปลี่ยนชื่อฟิลด์ใน `care-plan-confirm.html` ตามที่ผู้ใช้ขอ (ทั้งในกล่องร่าง AI และ
  ส่วนตัวอย่างสถานะหลังยืนยัน): "ความจำเป็นในการติดตาม" → "การวางแผนทางการพยาบาล", "สัญญาณความเสี่ยง" →
  "ประเด็นที่ต้องติดตาม" (id ของ field ในโค้ด `followUpNeed`/`riskSignals` คงเดิม ไม่กระทบการทำงาน).
- 2026-09-08 (รอบ 28): เพิ่มไฟล์ใหม่ `care-plan-print.html` — รายงานสรุปแผนการดูแลผู้ป่วยแบบพิมพ์ได้
  1 หน้า (A4) สำหรับพกไปประกอบการเยี่ยมบ้าน ไม่มี sidebar/nav (เป็นเอกสารเดี่ยว ไม่ใช่หน้าแอป) รวมข้อมูล
  ทั้งหมดที่ส่งต่อมา: ข้อมูลผู้ป่วย (HN/เลขบัตร/สิทธิการรักษา/สถานะผู้ป่วย/ที่อยู่/ผู้ดูแลหลัก), แหล่งที่มา
  และการส่งต่อ (แหล่งที่มา/ประเภทเคส/แพทย์เจ้าของไข้/วันที่ Admit-จำหน่าย-นัดติดตาม), ข้อมูลทางคลินิก
  (การวินิจฉัย/โรคประจำตัว/ประวัติผ่าตัด/อุปกรณ์ผู้ป่วย/PPS Score), และแผนที่ยืนยันแล้ว: "การวางแผนทาง
  การพยาบาล" + "ประเด็นที่ต้องติดตาม" + กำหนดการเยี่ยมครั้งแรก พร้อมช่องลงชื่อผู้ยืนยันแผนและที่ว่างสำหรับ
  จดบันทึกด้วยลายมือระหว่างเยี่ยม ใช้ `@media print` ซ่อนแถบเครื่องมือ/ปุ่มพิมพ์ และตั้ง `@page{size:A4}`
  ลดขนาดตัวอักษร/ระยะห่างเฉพาะตอนพิมพ์ให้พอดี 1 หน้า — ทดสอบแล้วเนื้อหาทั้งหมดสั้นกว่าความสูง 1 หน้าจอ
  มาก จึงน่าจะพอดี 1 แผ่น A4 เมื่อพิมพ์จริง (ยืนยันชัวร์ต้องลองพิมพ์จริงจากเบราว์เซอร์อีกที)
  เพิ่มปุ่ม "🖨️ พิมพ์รายงานสรุปแผนการดูแล" ใน confirmed-box ของ `care-plan-confirm.html` ลิงก์ไปหน้านี้.
- 2026-09-08 (รอบ 29): เปลี่ยนชื่อหน้า "ยืนยันแผนการดูแล (AI ช่วยร่าง)" เป็น "วิเคราะห์แผนการพยาบาล"
  ให้ตรงกับข้อความปุ่มที่นำมาหน้านี้ (เปลี่ยนไปตั้งแต่รอบ 16) — แก้ทั้ง `<title>`, breadcrumb "current",
  และ h1 ใน `care-plan-confirm.html`, กับลิงก์ย้อนกลับใน `care-plan-print.html`.
- 2026-09-08 (รอบ 30): สลับลำดับ "การจำแนกผู้ป่วยตามระดับความรุนแรง" กับ "สถานะผู้ป่วย" ใน
  `referral-create.html` ตามที่ผู้ใช้แจ้งหลังทดสอบ — ลำดับใหม่: ประเภทผู้ป่วย → การจำแนกกลุ่มความรุนแรง →
  สถานะผู้ป่วย → หน่วยต้นสังกัด (conditional, ยังคงอยู่ติดกับสถานะผู้ป่วยเหมือนเดิมเพราะ toggle ผูกกับ
  ฟิลด์นี้) ทดสอบแล้ว toggle หน่วยต้นสังกัดยังทำงานถูกต้องหลังสลับตำแหน่ง.
- 2026-09-08 (รอบ 31): เพิ่มฟิลด์ประเมินทางคลินิกในหน้า `followup-record.html` (บันทึกผลติดตาม) ตามที่
  ผู้ใช้ขอ วางไว้ระหว่างวันเวลาที่เยี่ยมกับ PPS Score เดิม:
  - "ลักษณะทั่วไป (General Appearance)" — ช่องข้อความอิสระ
  - "สัญญาณชีพ (Vital Signs)" — BP/PR/RR/Temp/SpO2 เป็น mini-field แถวเดียว
  - "น้ำหนัก/ส่วนสูง/BMI" — กรอกน้ำหนัก-ส่วนสูง แล้วคำนวณ BMI + หมวด (ผอม/ปกติ/เกิน/อ้วน) อัตโนมัติ
  - "การประเมิน ADL (Activities of Daily Living)" — เพิ่มหลัง PPS Score, แสดงเฉพาะผู้ป่วยกลุ่ม 3 บ้านสีแดง
    (ตามที่ระบุ) ประเมินแบบย่อคล้าย Barthel ADL 4 หมวด (รับประทานอาหาร/เคลื่อนไหว-ย้ายตัว/ขับถ่าย-ปัสสาวะ/
    อาบน้ำ-แต่งตัว) แต่ละหมวดให้คะแนน 0-2 รวมแสดงผลอัตโนมัติ (X/8)
  เพิ่ม chip "กลุ่ม 3 — บ้านสีแดง" ใน patient-strip ของหน้านี้เพื่อให้บริบทตรงกับที่ทำให้ ADL แสดงผล (เคส
  ตัวอย่างหน้านี้ "คุณสมชาย ใจบุญ" เดิมเป็น Palliative Care ก็ยังคงเป็นได้ — ประเภทเคสกับกลุ่มความรุนแรง
  เป็นคนละมิติข้อมูลตามที่ตกลงกันไว้ในรอบ 21) ทดสอบ syncBmi()/syncAdl() แล้วคำนวณถูกต้อง.
- 2026-09-08 (รอบ 32): เพิ่มฟิลด์ "วิธีการติดตามครั้งนี้" แยกออกมาเป็นฟิลด์แรกสุดของฟอร์มใน
  `followup-record.html` — toggle 2 ตัวเลือก "ลงพื้นที่เยี่ยม"/"โทรติดตาม" (ปุ่มการ์ดคู่ คล้าย radio-card
  แต่เขียน CSS ใหม่เฉพาะไฟล์นี้เพราะไฟล์นี้ยังไม่มี component นี้มาก่อน) พร้อม hint อธิบายว่าอาจต่างจากที่
  วางแผนไว้ได้ (เช่น วางแผนลงพื้นที่แต่ติดสถานการณ์ต้องเปลี่ยนเป็นโทรแทน) — chip "เยี่ยมบ้าน" เดิมใน
  patient-strip ยังคงไว้เป็นข้อมูล "แผนเดิม" ไม่ได้ลบ ส่วนฟิลด์ใหม่นี้คือค่าที่บันทึกจริงของครั้งนี้
  ทดสอบคลิกสลับ 2 ตัวเลือกแล้วสถานะ checked/UI ทำงานถูกต้อง.
  หมายเหตุที่ยังไม่ได้ทำ (นอกขอบเขตที่ขอ): ฟิลด์ตรวจร่างกาย (ลักษณะทั่วไป/V-S/น้ำหนัก-ส่วนสูง-BMI) ยังคง
  แสดงเสมอไม่ว่าจะเลือกวิธีไหน ทั้งที่ตามจริงข้อมูลกลุ่มนี้เก็บทางโทรศัพท์ไม่ได้ — ถ้าต้องการให้ซ่อน/แสดง
  ตามวิธีที่เลือก แจ้งได้.

**Known prototype simplifications (not bugs):**
- All referral rows in `referrals-list.html` link to the same `referral-detail.html` (one mock patient),
  and all admin list rows link to a single shared edit-form file — static prototypes don't have per-ID
  routing.
- Each screen batch was built by a separate agent; a link-consistency pass was done afterward to align
  sidebar nav filenames across all 12 files (a few agents initially invented slightly different filenames
  for sibling screens before the full set existed).
