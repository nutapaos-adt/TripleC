<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Ollama (Local LLM) Connection
    |--------------------------------------------------------------------------
    |
    | ต้องเป็น URL ภายในเครือข่าย รพ. เท่านั้น (ไม่ใช่ cloud API) เพราะข้อมูลที่ส่งไป
    | ประมวลผลเป็นข้อมูลผู้ป่วย (PHI) — ห้ามชี้ไปยังบริการภายนอกอินเทอร์เน็ต
    |
    */
    'ollama' => [
        'url' => env('OLLAMA_URL', 'http://127.0.0.1:11434'),
        'model' => env('OLLAMA_MODEL', 'typhoon'),
        'timeout' => (int) env('OLLAMA_TIMEOUT', 60),
    ],
];
