# DESIGN.md — Chira Continuity Care (Triple C)

Design system อ้างอิงสำหรับทีมพัฒนา ใช้เป็นแหล่งความจริงเดียว (single source of truth) สำหรับสี ตัวอักษร
ระยะห่าง คอมโพเนนต์ และกฎ UX ของระบบ Chira Continuity Care (Triple C) — ระบบติดตามเยี่ยมบ้านและดูแลต่อเนื่อง

> เอกสารนี้ควบคุมหน้าตา "แอปที่ใช้งานจริง" (in-product UI) เป็นหลัก ส่วนโลโก้/ตราสัญลักษณ์ยังคงใช้เฉดสีเดิม
> ตามที่ออกแบบไว้ — รายละเอียดว่าทำไมสองส่วนนี้ใช้สีคนละชุดกันอธิบายไว้ในหัวข้อที่ 1

---

## สารบัญ

1. [Brand Identity & CI](#1-brand-identity--ci)
2. [Design Tokens](#2-design-tokens)
3. [UI Components & Patterns](#3-ui-components--patterns)
4. [UX Guidelines & Rules](#4-ux-guidelines--rules)

---

## 1. Brand Identity & CI

### 1.1 ชื่อและความหมาย

- **ชื่อเต็ม:** Chira Continuity Care
- **ชื่อเล่น/ที่ใช้เรียกในทีม:** Triple C
- **ความหมาย:** "Continuity" (ความต่อเนื่อง) คือหัวใจของระบบ — เกิดขึ้นเพื่อทดแทนช่องว่างที่เกิดจากการยกเลิก
  ระบบ Thai COC โดยทำหน้าที่เชื่อมข้อมูลผู้ป่วยจากหอผู้ป่วย/OPD/หน่วยงานภายใน/โรงพยาบาลอื่น ไปจนถึงการติดตาม
  เยี่ยมบ้านและปิดเคสอย่างไม่ขาดตอน

### 1.2 โลโก้

โลโก้ประกอบด้วยวงแหวน 3 วง (สื่อถึงตัว "C" ทั้ง 3 ใน Triple C) สอดประสานกันเป็นห่วงโซ่เดียว วางบนพื้นสีครีมอุ่น:

| องค์ประกอบ | ความหมาย |
|---|---|
| วงแหวนสีน้ำเงิน (ซ้าย) | จุดรับข้อมูล — หอผู้ป่วย/OPD/หน่วยงานภายใน/โรงพยาบาลอื่น |
| วงแหวนสีเขียว (กลาง) | ทีมเยี่ยมบ้าน — จุดประมวลผลและติดตามดูแล |
| วงแหวนสีทอง (ขวา) | ผู้ป่วยที่บ้าน — ปลายทางของการดูแลต่อเนื่อง |
| การสอดประสานกันของวงแหวน | ความต่อเนื่องของข้อมูล ไม่มีจุดขาดตอนระหว่างส่งต่อ (แก้ปัญหาที่ระบบเดิมขาดหายไป) |
| ใบไม้เล็กๆ ที่ปลายวง | การดูแล/การเติบโต/ความเป็นธรรมชาติของการดูแลถึงบ้าน |

**กฎการใช้โลโก้**

- พื้นที่ว่างขั้นต่ำรอบโลโก้ (clear space) ≥ ความสูงของตัว "C" หนึ่งวง ห้ามมีองค์ประกอบอื่นล้ำเข้ามา
- ขนาดเล็กสุดที่ใช้ได้: 32px (ความสูง) สำหรับ favicon/ไอคอนแอป, 96px สำหรับใช้พร้อมคำว่า "TRIPLE C"
- ห้ามเปลี่ยนสีวงแหวนทั้ง 3 เป็นสีอื่น ห้ามใช้เป็น monochrome ยกเว้นกรณีพิมพ์ขาวดำเอกสารราชการ
- ห้ามยืด/บีบสัดส่วน ห้ามหมุน ห้ามใส่เงา/เอฟเฟกต์เพิ่มเติมนอกเหนือจากไฟล์ต้นฉบับ
- บนพื้นเข้ม ให้ใช้เวอร์ชันที่มีพื้นหลังครีมเป็นแผ่นรองเสมอ (ไม่วางโลโก้ลอยบนพื้นเข้มตรงๆ)
- ไฟล์ต้นฉบับ: เก็บไว้ที่ [`resources/branding/logo.png`](resources/branding/logo.png) (ไฟล์ความละเอียดสูง
  2816×1536px) — ถ้ามีเวอร์ชันเวกเตอร์ (SVG) ในอนาคต ให้เพิ่มเป็น `resources/branding/logo.svg` ในตำแหน่งเดียวกัน

### 1.3 ทำไม UI ในแอปใช้ "โทนเขียวทหาร" ไม่ใช่สีจากโลโก้ตรงๆ

โลโก้ใช้ 3 สี (น้ำเงิน/เขียว/ทอง) เพื่อสื่อความหมายเชิงสัญลักษณ์ของกระบวนการ แต่หน้าจอที่ใช้งานจริงทุกวัน
ต้องมีสีหลักเดียวที่ชัดเจน สม่ำเสมอ และสื่อบุคลิกของทีมเยี่ยมบ้าน — ทีมงานที่ออกไปปฏิบัติภารกิจถึงบ้านผู้ป่วย
ไม่ต่างจากหน่วยปฏิบัติการภาคสนาม จึงเลือก **โทนเขียวทหาร/โอลีฟ (Olive/Military Green)** เป็นสีหลักของระบบ
สื่อถึงความน่าเชื่อถือ ความมั่นคง การปฏิบัติหน้าที่ และความพร้อมออกภาคสนาม — สีน้ำเงินและทองจากโลโก้ยังคงถูก
เก็บไว้เป็น **สีรอง (secondary accent)** ใช้แบบประหยัดในหน้า login/แบรนด์ดิ้ง ไม่ใช้เป็นสีปุ่ม/ลิงก์หลักในแอป

### 1.4 บุคลิกแบรนด์ (Brand Personality)

| คุณสมบัติ | หมายถึง |
|---|---|
| **Reliable (เชื่อถือได้)** | ข้อมูลผู้ป่วยต้องแม่นยำ ไม่มีการคาดเดาที่ไม่มีมนุษย์ยืนยัน |
| **Field-ready (พร้อมภาคสนาม)** | ใช้งานได้จริงบนมือถือ/แท็บเล็ตขณะออกเยี่ยมบ้าน ไม่ใช่แค่บนโต๊ะทำงาน |
| **Human-first (คนเป็นศูนย์กลาง)** | AI ช่วยงาน แต่พยาบาล/เจ้าหน้าที่คือผู้ตัดสินใจเสมอ |
| **Precise (แม่นยำ)** | ตัวเลข วันที่ สถานะ ต้องอ่านง่ายและถูกต้อง โดยเฉพาะข้อมูลทางคลินิก |

### 1.5 น้ำเสียงการเขียน (Voice & Tone)

- ใช้ภาษาไทยที่เป็นทางการพอสมควร (บริบทการแพทย์) แต่**อ่านง่าย ไม่ใช้ศัพท์เทคนิคเกินจำเป็น**
- เรียกสิ่งต่างๆ ตามที่ผู้ใช้เข้าใจ ("ใบส่งต่อ" ไม่ใช่ "referral record", "เขต" ไม่ใช่ "zone flag")
- ปุ่ม/การกระทำใช้กริยาที่ตรงไปตรงมา ("ยืนยันแผนติดตาม" ไม่ใช่ "ดำเนินการต่อ")
- ข้อความแจ้งเตือน/error บอกสาเหตุและวิธีแก้เสมอ ไม่ใช้คำขอโทษพร่ำเพรื่อหรือคำกำกวม

---

## 2. Design Tokens

### 2.1 สี (Color)

#### Primary — Olive / Military Green (สีหลักของระบบ)

ใช้สำหรับปุ่มหลัก ลิงก์ สถานะ active สี highlight ทั่วไป

| Token | Hex | การใช้งาน |
|---|---|---|
| `--color-primary-900` | `#212B1C` | ข้อความเข้มบนพื้นอ่อน, sidebar เข้มสุด |
| `--color-primary-800` | `#2C3A22` | sidebar background |
| `--color-primary-700` | `#3C4D2A` | **สีหลัก** — ปุ่ม primary, ลิงก์, ไอคอนสำคัญ |
| `--color-primary-600` | `#4C5F35` | ปุ่ม primary ตอน hover |
| `--color-primary-500` | `#62784A` | ไอคอน/องค์ประกอบรอง |
| `--color-primary-300` | `#AAB994` | border ของกล่อง "ร่างจาก AI" |
| `--color-primary-100` | `#E9EDDF` | พื้นหลังกล่องไฮไลต์อ่อนๆ, hover state |
| `--color-primary-50`  | `#F5F6EF` | พื้นหลังหน้าเว็บโดยรวม |

#### Neutral — Warm Khaki (โทนเทาอุ่นแบบผ้าแคนวาส/เครื่องแบบ)

| Token | Hex | การใช้งาน |
|---|---|---|
| `--color-neutral-900` | `#22201A` | ข้อความหลัก |
| `--color-neutral-700` | `#4A4739` | ข้อความรอง/label |
| `--color-neutral-500` | `#7C7863` | ข้อความ disabled/placeholder |
| `--color-neutral-300` | `#C9C4AD` | เส้นขอบ (border) |
| `--color-neutral-200` | `#DEDAC6` | เส้นแบ่ง (divider) |
| `--color-neutral-100` | `#F1EEE0` | พื้นหลัง section สลับ (zebra/sunken bg) |
| `--color-surface`     | `#FFFFFF` | พื้นหลังการ์ด/ฟอร์ม |

#### Secondary Accent — จากโลโก้ (ใช้แบบประหยัด)

| Token | Hex | การใช้งาน |
|---|---|---|
| `--color-accent-blue` | `#3E7CA6` | จุดรับข้อมูล/แหล่งที่มา, หน้า login/แบรนด์ดิ้งเท่านั้น |
| `--color-accent-gold` | `#C9932E` | ไฮไลต์พิเศษ, หน้า login/แบรนด์ดิ้งเท่านั้น |

#### Semantic — สถานะ (ต้องแยกจาก Primary ให้ชัดเจน)

| Token | Hex | ใช้กับ |
|---|---|---|
| `--color-risk`    | `#B23B2C` (rust red) | สัญญาณเสี่ยง, เกินกำหนด, error |
| `--color-risk-tint`    | `#F7E7E2` | พื้นหลังชิป/แบนเนอร์เสี่ยง |
| `--color-warning` | `#C2891F` (mustard) | รอถึงกำหนด, ต้องตรวจสอบ |
| `--color-warning-tint` | `#FBF0DC` | พื้นหลังชิป/แบนเนอร์เตือน |
| `--color-success` | `#3E8E49` (grass green) | เยี่ยมแล้ว, ยืนยันแล้ว, สำเร็จ |
| `--color-success-tint` | `#E5F2E4` | พื้นหลังชิป/แบนเนอร์สำเร็จ |

> ⚠️ **กฎสำคัญ:** `--color-success` (#3E8E49) ต้องแยกแยะจาก `--color-primary-700` (#3C4D2A) ได้ด้วยตา —
> primary เป็นโทนหม่น/โอลีฟ ส่วน success เป็นเขียวสดกว่าอย่างมีนัยสำคัญ ห้ามใช้ปนกันเป็นสีเดียว

#### Dark Mode (เตรียมไว้สำหรับอนาคต — เจ้าหน้าที่เยี่ยมบ้านช่วงเย็น/กลางคืน)

| Token | Hex |
|---|---|
| `--color-bg-dark` | `#1A1D15` |
| `--color-surface-dark` | `#23271C` |
| `--color-text-dark` | `#ECE9DD` |
| `--color-primary-dark` | `#9CB37E` (สว่างขึ้นเพื่อ contrast บนพื้นเข้ม) |

### 2.2 ตัวอักษร (Typography)

**Font stack** (ภาษาไทยเป็นหลัก — ใช้ font ระบบเพื่อความชัดเจนของกลุ่มอักษรไทยและลดขนาดไฟล์):

```css
font-family: 'Sarabun', 'Noto Sans Thai', 'Leelawadee UI', 'Segoe UI', system-ui, sans-serif;
```

**Type Scale**

| ระดับ | ขนาด/บรรทัด | น้ำหนัก | ใช้กับ |
|---|---|---|---|
| `display` | 32px / 40px | 800 | ตัวเลข KPI บนแดชบอร์ด (ใช้ `font-variant-numeric: tabular-nums`) |
| `h1` | 24px / 32px | 700 | หัวข้อหน้า |
| `h2` | 20px / 28px | 700 | หัวข้อ section/การ์ด |
| `h3` | 16px / 24px | 600 | หัวข้อย่อย |
| `body-lg` | 16px / 26px | 400 | เนื้อหาสำคัญ (ข้อความสรุปอาการ) |
| `body` | 14px / 22px | 400 | เนื้อหาทั่วไป, ฟอร์ม |
| `label` | 12.5px / 18px | 600 | label ฟอร์ม, eyebrow |
| `caption` | 11.5px / 16px | 500 | timestamp, ข้อความประกอบเล็ก |

**กฎ:** ความกว้างข้อความเนื้อหา (body) ไม่ควรเกิน ~70 ตัวอักษรต่อบรรทัดเพื่อการอ่านสบายตา

### 2.3 ระยะห่าง (Spacing)

หน่วยฐาน 4px — ใช้ scale นี้แทนตัวเลขสุ่มเสมอ (ผ่าน `gap`/`padding` ไม่ใช่ margin สะสม):

| Token | ค่า |
|---|---|
| `--space-1` | 4px |
| `--space-2` | 8px |
| `--space-3` | 12px |
| `--space-4` | 16px |
| `--space-5` | 20px |
| `--space-6` | 24px |
| `--space-8` | 32px |
| `--space-10` | 40px |
| `--space-12` | 48px |
| `--space-16` | 64px |

### 2.4 Radius & Shadow

| Token | ค่า | ใช้กับ |
|---|---|---|
| `--radius-sm` | 4px | ชิป, input, ปุ่มเล็ก |
| `--radius-md` | 8px | ปุ่ม, การ์ดทั่วไป |
| `--radius-lg` | 12px | การ์ดใหญ่/panel |
| `--radius-pill` | 999px | badge สถานะ |

> แนวทาง: ระบบนี้เน้นความรู้สึก "ปฏิบัติการ/กระชับ" ไม่ใช่ความนุ่มนวลแบบ consumer app — **ใช้ radius เล็ก-กลาง
> (4–12px) เป็นหลัก หลีกเลี่ยงการทำมุมโค้งมนใหญ่ทุกองค์ประกอบ**

| Token | ค่า |
|---|---|
| `--shadow-sm` | `0 1px 2px rgba(34,32,26,0.06)` |
| `--shadow-md` | `0 1px 2px rgba(34,32,26,0.06), 0 4px 14px rgba(34,32,26,0.08)` |

---

## 3. UI Components & Patterns

### 3.1 ปุ่ม (Buttons)

| ประเภท | สี | ใช้เมื่อ |
|---|---|---|
| Primary | พื้น `primary-700`, ตัวหนังสือขาว | การกระทำหลักของหน้า (บันทึก, ยืนยัน) — มีได้ปุ่มเดียวต่อหน้า |
| Secondary | พื้นขาว, ขอบ `neutral-300`, ตัวหนังสือ `neutral-900` | การกระทำรอง (ยกเลิก, ขอ AI สรุปใหม่) |
| Danger | พื้น `risk`, ตัวหนังสือขาว | การกระทำที่ย้อนกลับไม่ได้ (ปิดเคส, ลบ) — ต้องมี confirm dialog เสมอ |

### 3.2 Badge / Status Chip

ใช้แสดงสถานะแบบมองปราดเดียวเข้าใจ **ต้องมีทั้งสีและข้อความเสมอ ห้ามใช้สีอย่างเดียว**

```
[ในเขต]        bg: primary-100   text: primary-700
[นอกเขต]       bg: neutral-100   text: neutral-700
[เกินกำหนด]     bg: risk-tint     text: risk
[รอถึงกำหนด]    bg: warning-tint  text: warning
[เยี่ยมแล้ว]     bg: success-tint  text: success
```

### 3.3 AI-Draft Pattern (สำคัญที่สุดของระบบนี้)

ทุกครั้งที่ AI สร้างข้อมูล (สรุปเคส / คู่มือติดตาม / วิเคราะห์ความเสี่ยง) ต้องแสดงด้วย pattern นี้เท่านั้น:

- กรอบ **เส้นประ** สี `primary-300` พื้นหลัง `primary-50`
- ป้ายกำกับตัวพิมพ์เล็กมุมซ้ายบน พื้น `primary-100` ตัวหนังสือ `primary-700`: **"ร่างจาก AI — ยังไม่ยืนยัน"**
- ทุกช่องข้อมูลต้อง**แก้ไขได้** (ไม่ใช่ read-only)

เมื่อพยาบาลยืนยันแล้ว ให้เปลี่ยนเป็น:

- กรอบ**เส้นทึบ** สี `primary-700`
- ป้ายกำกับเปลี่ยนเป็น **"ยืนยันแล้วโดย [ชื่อ] เมื่อ [วันที่-เวลา]"**

### 3.4 Nurse-Decision Pattern

จุดที่พยาบาลต้องตัดสินใจ (ติดตามซ้ำ/ส่งต่อ/ปิดเคส) ต้องแยกจากกล่อง AI อย่างชัดเจน:

- กรอบเส้นทึบหนา สี `primary-700` (หนากว่ากรอบทั่วไป)
- ป้ายกำกับ: **"การตัดสินใจของพยาบาล — ต้องยืนยันเสมอ"**
- ปุ่มตัวเลือกแบบ radio card (ไม่ใช่ dropdown) เพื่อให้เห็นตัวเลือกทั้งหมดพร้อมกัน

### 3.5 KPI Stat Tile (แดชบอร์ด)

การ์ดสีขาว, label ขนาด caption สีเทา, ตัวเลขขนาด `display` — ใช้สี semantic กับตัวเลขเมื่อค่านั้นผิดปกติ
เท่านั้น (เช่น "เกินกำหนด" > 0 ให้เป็นสี risk, ปกติเป็น `neutral-900`)

### 3.6 Timeline (ประวัติผู้ป่วย)

เส้นตั้งสี `neutral-300`, จุด (dot) วงกลมขอบ `primary-700` — เหตุการณ์ที่มีความเสี่ยงใช้ขอบจุดสี `risk`,
เหตุการณ์ในอนาคต (ยังไม่เกิด) ใช้จุดเส้นประสี `neutral-500`

### 3.7 Sidebar Navigation

พื้นหลังไล่ระดับสีเขียวเข้ม (`primary-900` → `primary-800`) ตัวหนังสือสีครีมอ่อน รายการ active ใช้พื้น
`primary-700` โปร่งแสง 15–20% — สะท้อนบุคลิก "หน่วยปฏิบัติการ" ตามหัวข้อ 1.3

### 3.8 ฟอร์ม (Forms)

- Label ใช้ระดับ `label` (12.5px/600) อยู่เหนือ input เสมอ ไม่ใช้ placeholder แทน label
- Input พื้นหลัง `neutral-100`, ขอบ `neutral-300`, focus ring สี `primary-700`
- ข้อความช่วยเหลือ (hint) ใช้สีเทา `neutral-500` ขนาด `caption` อยู่ใต้ label

### 3.9 ตาราง (Tables)

Header ตัวพิมพ์เล็กสีเทา ตัวหนา, แถวคั่นด้วยเส้น `neutral-200` บางๆ ไม่ใช้เส้นตาราง (grid lines) เต็มรูปแบบ
ตัวเลขที่เรียงคอลัมน์ (วันที่, คะแนน) ต้องใช้ `tabular-nums`

---

## 4. UX Guidelines & Rules

### 4.1 Human-in-the-loop คือกฎข้อที่ 1 (ไม่มีข้อยกเว้น)

ทุกจุดที่ AI ประมวลผล (สรุปข้อมูล, แนะนำแผน, เตรียมคู่มือ, วิเคราะห์ความเสี่ยง) **ต้องมีมนุษย์ตรวจสอบและกดยืนยัน
ก่อนใช้งานจริงเสมอ 100%** ห้ามมีเส้นทางใดที่ AI ตัดสินใจแทนหรือบันทึกผลลงระบบโดยอัตโนมัติโดยไม่มีคนยืนยัน —
นี่คือกฎความปลอดภัยผู้ป่วยที่สำคัญที่สุดของระบบ (ดู pattern 3.3–3.4)

### 4.2 สื่อสารสถานะให้ชัดที่สุด

- ทุกสถานะต้องมีทั้งสี**และ**ข้อความกำกับ (accessibility — ห้ามพึ่งสีอย่างเดียว)
- ค่าที่ผิดปกติ/ต้องการความสนใจ (เกินกำหนด, พบความเสี่ยง) ต้องอยู่**ตำแหน่งบนสุดของหน้า** ไม่ซ่อนอยู่ท้ายตาราง

### 4.3 ภาษาและการเขียน UI

- ทุกข้อความในระบบเป็นภาษาไทย เขียนแบบที่บุคลากรทางการแพทย์ใช้จริง ไม่ใช่ศัพท์วิศวกรรม
- ปุ่มบอกผลลัพธ์ที่จะเกิดขึ้นตรงๆ ("ยืนยันแผนติดตาม" ไม่ใช่ "ตกลง")
- ข้อความ error ต้องบอกว่าเกิดอะไรและควรทำอะไรต่อ (เช่น "เรียกใช้ AI ไม่สำเร็จ กรุณากรอกด้วยตนเอง")

### 4.4 ออกแบบเพื่อภาคสนามก่อน (Field-first)

- ทุกหน้าที่ทีมเยี่ยมบ้านใช้ระหว่างออกเยี่ยม (คู่มือติดตาม, บันทึกผล) ต้องใช้งานได้ดีบนจอมือถือ/แท็บเล็ต
  ปุ่มหลักต้องใหญ่พอสำหรับการแตะ ไม่ต้องพึ่ง hover
- ฟอร์มบันทึกผลควรกรอกให้เสร็จได้แม้สัญญาณอินเทอร์เน็ตไม่เสถียร (ฟิลด์สั้น กระชับ ไม่บังคับกรอกเกินจำเป็น)

### 4.5 ความปลอดภัยของข้อมูลผู้ป่วย (PHI)

- ห้ามแสดงหรือส่งข้อมูลผู้ป่วยผ่าน query string ของ URL
- ทุกหน้าที่มีข้อมูลผู้ป่วยต้องอยู่หลัง authentication เสมอ ไม่มีหน้า public ที่เห็นข้อมูลเคสได้
- การเรียก AI ทุกครั้งต้องผ่าน endpoint ภายในเครือข่าย รพ. เท่านั้น (ดู `config/ai.php`) ห้ามส่งข้อมูลผู้ป่วย
  ออกไปยัง API ภายนอกอินเทอร์เน็ตโดยเด็ดขาด

### 4.6 Accessibility

- ทุก element ที่กดได้ต้องมี focus state ที่มองเห็นชัด (ไม่ลบ outline โดยไม่ใส่ทดแทน)
- คอนทราสต์ตัวหนังสือต่อพื้นหลังต้องผ่านเกณฑ์ WCAG AA เป็นอย่างต่ำ (โทนเขียวทหารเข้มบนพื้นครีมผ่านเกณฑ์นี้แล้ว
  แต่ต้องระวังเมื่อใช้สี `primary-500` หรืออ่อนกว่าเป็นตัวหนังสือบนพื้นขาว — ควรใช้กับพื้นหลังเข้มเท่านั้น)
- ไม่ใช้ animation ที่กะพริบเร็วหรือรบกวนสมาธิระหว่างทำงานกับข้อมูลคลินิก

### 4.7 ความสม่ำเสมอ (Consistency)

- ชื่อ field/label เดียวกันต้องใช้คำเดียวกันทุกหน้า (เช่น "ครั้งที่", "เขตพื้นที่", "PPS Score")
- ลำดับขั้นตอนในทุก workflow ต้องเรียงจากบนลงล่าง/ซ้ายไปขวาเสมอ ไม่สลับทิศทาง
- ปุ่ม primary อยู่ตำแหน่งซ้ายสุดของกลุ่มปุ่มเสมอ ปุ่ม secondary/ยกเลิกอยู่ถัดไปทางขวา
